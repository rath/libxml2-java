SRC=src/main/c
TARGET=@JNILIB@

all: 
	cd $(SRC) && $(MAKE) $@

clean: 
	cd $(SRC) && $(MAKE) $@
	rm -f $(TARGET)

install: all
	cp $(SRC)/$(TARGET) $(TARGET)

Makefile: Makefile.in config.status
	./config.status $@

config.status: configure
	./config.status --recheck

.PHONY: all clean

